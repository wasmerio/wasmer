query GetSignedUrl(
  $name: String
  $version: String
  $filename: String
  $expiresAfterSeconds: Int
) {
  url: getSignedUrlForPackageUpload(
    name: $name
    version: $version
    expiresAfterSeconds: $expiresAfterSeconds
  ) {
    url
  }
}
