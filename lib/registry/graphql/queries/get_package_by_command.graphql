query GetPackageByCommandQuery ($commandName: String!) {
  getCommand(name: $commandName) {
    command
    packageVersion {
      version
      distribution {
        downloadUrl
      }
      package {
        displayName
      }
    }
  }
}