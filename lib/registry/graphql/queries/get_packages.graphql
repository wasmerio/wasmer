query GetPackagesQuery ($names: [String!]!) {
  package: getPackages(names:$names) {
    name
    private
    versions {
      version
      distribution {
        downloadUrl
      }
      manifest
    }
  }
}
