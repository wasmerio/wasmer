[package]
name = "wasmer-swift"
version = "0.601.0"
description = "Experimental wasmer bindings for swift"
edition.workspace = true
license.workspace = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
crate-type = ["staticlib", "lib"]
name = "wasmer_swift"

[dependencies]
thiserror.workspace = true
tokio = { workspace = true, features = ["rt"], default-features = false }
uniffi.workspace = true
virtual-fs = { workspace = true, default-features = false, features = [
	"webc-fs",
] }
wasmer = { workspace = true, default-features = false, features = [
	"wamr",
	"std",
] }
wasmer-wasix = { workspace = true, default-features = false, features = [
	"sys",
] }
webc.workspace = true
wasmer-package.workspace = true


[build-dependencies]
uniffi = { workspace = true, features = ["build"] }

[package.metadata.docs.rs]
features = ["wasmer/sys", "wasmer/compiler"]
rustc-args = ["--cfg", "docsrs"]
